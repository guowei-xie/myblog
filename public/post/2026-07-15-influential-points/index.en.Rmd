---
title: 回归诊断中的强影响点：从标准化残差到影响图
author: xiebro
date: '2026-07-15'
slug: []
categories:
  - R
tags:
  - Regression
  - Diagnostics
  - Influential-Points
math: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  warning = FALSE,
  message = FALSE,
  fig.width = 7.4,
  fig.height = 4.4,
  dpi = 120,
  fig.retina = 2
)
```

```{r theme, include=FALSE}
library(ggplot2)

palette_blog <- c(
  ink     = "#1F2933",
  blue    = "#2C5F8D",
  rose    = "#D7846A",
  sage    = "#7FAA7A",
  gold    = "#E0B36A",
  plum    = "#5D4E75",
  ash     = "#94A3B0"
)

theme_blog <- function(base_size = 12) {
  theme_minimal(base_size = base_size) +
    theme(
      plot.title          = element_text(face = "bold", size = rel(1.15),
                                         color = palette_blog["ink"],
                                         margin = margin(b = 4)),
      plot.subtitle       = element_text(size = rel(0.92),
                                         color = palette_blog["ash"],
                                         margin = margin(b = 14)),
      plot.title.position = "plot",
      plot.caption        = element_text(size = rel(0.78),
                                         color = palette_blog["ash"],
                                         hjust = 0,
                                         margin = margin(t = 10)),
      plot.caption.position = "plot",
      axis.title          = element_text(size = rel(0.88),
                                         color = palette_blog["ink"]),
      axis.title.x        = element_text(margin = margin(t = 8)),
      axis.title.y        = element_text(margin = margin(r = 8)),
      axis.text           = element_text(color = palette_blog["ash"]),
      panel.grid.major    = element_line(color = "grey92", linewidth = 0.3),
      panel.grid.minor    = element_blank(),
      panel.spacing       = unit(1.2, "lines"),
      strip.text          = element_text(face = "bold",
                                         color = palette_blog["ink"],
                                         size = rel(0.95)),
      strip.background    = element_rect(fill = "grey96", color = NA),
      legend.position     = "top",
      legend.justification = "left",
      legend.title        = element_text(size = rel(0.88),
                                         color = palette_blog["ink"]),
      legend.text         = element_text(size = rel(0.85),
                                         color = palette_blog["ink"]),
      legend.margin       = margin(b = 4),
      plot.margin         = margin(16, 18, 14, 16)
    )
}
```

拟合出一条回归线只是开始。回归系数是全部样本共同决定的，如果其中一两个点位置特殊，它们对结果的话语权可能远大于其余点，甚至单独决定斜率的正负。回归诊断要查的就是这件事：有没有这样的点，影响有多大，要不要处理。

下面用一份模拟数据把几个常用的诊断量过一遍：每个指标衡量什么、经验阈值取多少、图怎么看，以及据此怎么决定。先讲三个最基础的，标准化残差、杠杆值、库克距离，再看把它们整合到一起的影响图；最后补上三个更细的删除诊断：DFFITS、DFBETAS、COVRATIO。

## 三种"不正常"的点

有三个概念容易混，先分清楚：

- **离群值（outlier）**：实际 $y$ 离预测值很远的点，残差大，属于 $y$ 方向上的异常。
- **高杠杆点（high leverage）**：自变量取值远离其余样本的点，属于 $x$ 方向上的异常；杠杆衡量的是这个点能对回归结果施加多大影响。
- **强影响点（influential point）**：删掉它，回归方程会明显改变。这样的点不一定有大残差。

一个点可以杠杆很高却几乎不影响结果，也可以残差很大却撼不动方程。最麻烦的是那种既在 $x$ 方向极端、又偏离整体趋势的点。下面就造出这三类点，看每个指标各自能抓到谁。

## 模拟数据

用一元线性回归来演示，好处是能把回归线直接画在散点图上，看得见线被带偏。这些指标对多元回归一样成立，只是没法再用二维散点图直接呈现。

真实关系设为 $y = 3 + 1.5x + \varepsilon$，先生成 40 个正常的点，再刻意加进三个特殊点 A、B、C：

```{r sim-data}
library(tidyverse)
set.seed(2026)

n_base  <- 40
x_base  <- runif(n_base, 4, 12)
y_base  <- 3 + 1.5 * x_base + rnorm(n_base, 0, 1.2)
base    <- tibble(x = x_base, y = y_base, label = "normal")

# B：x 居中、y 大幅偏离 —— 离群点
# C：x 偏大、y 明显偏离趋势 —— 强影响点
xB <- 7;  yB <- 3 + 1.5 * xB + 9
xC <- 13; yC <- 7

# A：x 最极端（高杠杆），但 y 落在"其余数据所隐含的那条线"上 —— 顺趋势
# 这样它虽然杠杆最高，加入后几乎不改变方程，是"好杠杆点"的典型
rest    <- bind_rows(base, tibble(x = c(xB, xC), y = c(yB, yC), label = c("B", "C")))
xA      <- 17
yA      <- as.numeric(predict(lm(y ~ x, data = rest),
                              newdata = tibble(x = xA))) + 0.6

special <- tibble(label = c("A", "B", "C"),
                  x = c(xA, xB, xC),
                  y = c(yA, yB, yC))

dat <- bind_rows(base, special) |> mutate(obs = row_number())
n   <- nrow(dat)   # 样本量
p   <- 1           # 预测变量个数（系数个数为 p + 1）
```

先拟合全量回归，并和"剔除 C 之后"的回归放在一张图上对比：

```{r fig-scatter, fig.height=4.7}
fit    <- lm(y ~ x, data = dat)
fit_noC <- lm(y ~ x, data = filter(dat, label != "C"))

pts <- dat |>
  mutate(kind = recode(label,
                       normal = "normal",
                       A = "A: high leverage",
                       B = "B: outlier",
                       C = "C: influential"))

lab_pts <- filter(pts, label != "normal")

ggplot(pts, aes(x, y)) +
  geom_point(data = filter(pts, label == "normal"),
             color = palette_blog["ash"], size = 2, alpha = 0.8) +
  geom_abline(intercept = coef(fit)[1], slope = coef(fit)[2],
              color = palette_blog["blue"], linewidth = 0.9) +
  geom_abline(intercept = coef(fit_noC)[1], slope = coef(fit_noC)[2],
              color = palette_blog["rose"], linewidth = 0.9, linetype = 2) +
  geom_point(data = lab_pts, aes(color = kind), size = 3.4) +
  geom_text(data = lab_pts, aes(label = label, color = kind),
            vjust = -1, fontface = "bold", show.legend = FALSE) +
  annotate("text", x = 15.5, y = 27, label = "fit: all data",
           color = palette_blog["blue"], hjust = 0, size = 3.4) +
  annotate("text", x = 15.5, y = 23.2, label = "fit: without C",
           color = palette_blog["rose"], hjust = 0, size = 3.4) +
  scale_color_manual(values = unname(palette_blog[c("sage", "gold", "rose")])) +
  labs(title = "Three kinds of unusual points",
       subtitle = "Dropping the single point C visibly rotates the regression line",
       x = "x", y = "y", color = NULL) +
  theme_blog()
```

三个点的位置各不相同：A 在最右端（$x$ 极端），但正好落在其余点连成的那条线上；B 的 $x$ 很普通，$y$ 却高高抬起；C 的 $x$ 偏大，$y$ 又明显偏低。看实线（全量）和虚线（剔除 C）之间的夹角：只去掉 C 一个点，斜率就从 `r sprintf("%.2f", coef(fit)[2])` 变到 `r sprintf("%.2f", coef(fit_noC)[2])`。一个点能不能左右回归线，和它残差大不大是两回事。

把五个诊断量一次性算出来备用，都来自 base R，不用额外装包：

```{r diagnostics}
d <- dat |>
  mutate(
    hat       = hatvalues(fit),
    std_resid = rstandard(fit),
    cooks     = cooks.distance(fit),
    dffits    = dffits(fit),
    covratio  = covratio(fit)
  )

thr <- list(
  hat     = 2 * (p + 1) / n,          # 杠杆值阈值
  cooks   = 4 / (n - p - 1),          # 库克距离阈值
  dffits  = 2 * sqrt((p + 1) / n),    # DFFITS 阈值
  dfbetas = 2 / sqrt(n),              # DFBETAS 阈值
  covr    = 3 * (p + 1) / n           # COVRATIO 阈值（偏离 1 的幅度）
)
```

先写一个通用的索引图函数：横轴是观测序号，纵轴是某个诊断量，把 A/B/C 三点标出来。

```{r index-plot-fn}
hl <- c(normal = unname(palette_blog["ash"]),
        A = unname(palette_blog["sage"]),
        B = unname(palette_blog["gold"]),
        C = unname(palette_blog["rose"]))

index_plot <- function(df, yvar, title, ylab, hlines) {
  ggplot(df, aes(obs, .data[[yvar]])) +
    geom_hline(yintercept = hlines, linetype = 2,
               color = palette_blog["ink"], linewidth = 0.4) +
    geom_segment(aes(xend = obs, yend = 0, color = label), linewidth = 0.4) +
    geom_point(aes(color = label, size = label != "normal")) +
    geom_text(data = filter(df, label != "normal"),
              aes(label = label, color = label),
              vjust = -0.9, fontface = "bold", size = 3.4, show.legend = FALSE) +
    scale_color_manual(values = hl, guide = "none") +
    scale_size_manual(values = c(1.6, 3), guide = "none") +
    labs(title = title, x = "observation index", y = ylab) +
    theme_blog()
}
```

## 指标一 · 标准化残差：抓离群值

标准化残差是残差除以它自己的标准误，可以读作"偏离回归线多少个标准误"：

$$
r_i = \frac{e_i}{\hat\sigma \sqrt{1 - h_{ii}}}
$$

常用它判定离群值，经验阈值取 $\pm 2.5$。R 里用 `rstandard()`：

```{r fig-stdresid, fig.height=3.6}
index_plot(d, "std_resid",
           "Standardized residuals catch the outlier",
           "standardized residual", hlines = c(-2.5, 2.5))
```

B 冲出了 $\pm 2.5$ 的阈值线（`r sprintf("%.2f", d$std_resid[d$label=="B"])`），C 更低，到 `r sprintf("%.2f", d$std_resid[d$label=="C"])`。但 A 的标准化残差只有 `r sprintf("%.2f", d$std_resid[d$label=="A"])`，完全正常。标准化残差只看 $y$ 方向的偏离，藏在 $x$ 极端处的高杠杆点 A 它是看不出来的。

## 指标二 · 杠杆值：抓 x 方向的极端

杠杆值来自帽子矩阵。多元回归可写成 $\hat{Y} = HY$，其中

$$
H = X (X^{\top} X)^{-1} X^{\top}
$$

之所以叫帽子矩阵，是因为它给 $Y$ 戴上帽子变成了 $\hat{Y}$。它的对角线元素 $h_{ii}$ 就是第 $i$ 个观测的杠杆值，衡量该点自变量取值有多极端。经验法则是超过 $2(p+1)/n$ 就算高杠杆。R 里用 `hatvalues()`：

```{r fig-leverage, fig.height=3.6}
index_plot(d, "hat",
           "Leverage catches the extreme-x points",
           "leverage (hat value)", hlines = thr$hat)
```

这次越线的是 A 和 C，残差很大的 B 反而贴在底部，因为它的 $x$ 太普通。杠杆值不看响应变量 $y$，只看 $x$ 有多极端，正好和标准化残差互补：一个管 $x$，一个管 $y$。但单看任何一个，都不足以判断一个点有没有影响。

## 指标三 · 库克距离：抓真正的强影响点

库克距离（Cook's distance）把前两者合到一起，同时考虑残差大小和杠杆：

$$
D_i = \frac{r_i^{2}}{p + 1} \cdot \frac{h_{ii}}{1 - h_{ii}}
$$

只有残差大、杠杆又高的点，$D_i$ 才会被两个因子同时放大。经验阈值取 $4/(n - p - 1)$。R 里用 `cooks.distance()`：

```{r fig-cooks, fig.height=3.6}
index_plot(d, "cooks",
           "Cook's distance isolates the truly influential point",
           "Cook's distance", hlines = thr$cooks)
```

这次 C 遥遥领先（$D_C =$ `r sprintf("%.2f", d$cooks[d$label=="C"])`），把阈值线远远甩在下面；B 只是勉强越线，残差大但杠杆低；杠杆最高的 A 库克距离几乎为 0（`r sprintf("%.3f", d$cooks[d$label=="A"])`），因为它顺着趋势，删不删都不改变方程。可见高杠杆并不等于强影响。

## 影响图：一张图看全

把标准化残差、杠杆值、库克距离放进同一张图，是回归诊断里很常用的一种画法：横轴放杠杆值，纵轴放标准化残差，点的大小对应库克距离，库克距离超阈值的点画成实心。这种图一般叫影响图（influence plot）或气泡图（bubble plot）：

```{r fig-bubble, fig.height=5}
d <- d |> mutate(flagged = cooks > thr$cooks)

ggplot(d, aes(hat, std_resid)) +
  geom_hline(yintercept = c(-2.5, 2.5), linetype = 2,
             color = palette_blog["ash"], linewidth = 0.4) +
  geom_vline(xintercept = thr$hat, linetype = 2,
             color = palette_blog["ash"], linewidth = 0.4) +
  geom_point(aes(size = cooks, fill = flagged, color = flagged),
             shape = 21, alpha = 0.75) +
  geom_text(data = filter(d, label != "normal"),
            aes(label = label), fontface = "bold",
            vjust = -1.1, size = 3.6, color = palette_blog["ink"]) +
  scale_size_area(max_size = 13, guide = "none") +
  scale_fill_manual(values = c(`TRUE` = unname(palette_blog["rose"]),
                               `FALSE` = "white"), guide = "none") +
  scale_color_manual(values = c(`TRUE` = unname(palette_blog["rose"]),
                                `FALSE` = unname(palette_blog["ash"])),
                     guide = "none") +
  annotate("text", x = thr$hat, y = 4.6,
           label = "leverage threshold", hjust = -0.05,
           color = palette_blog["ash"], size = 3.2) +
  coord_cartesian(ylim = c(-6.4, 5), clip = "off") +
  labs(title = "Influence (bubble) plot",
       subtitle = "x = leverage, y = standardized residual, bubble area = Cook's distance",
       x = "leverage (hat value)", y = "standardized residual") +
  theme_blog()
```

几个位置的含义：

- 横轴越靠右杠杆越高，纵轴越远离 0 残差越大，气泡越大库克距离越大。
- C 在右下角，杠杆高、负残差也大，气泡最大且填实，是最该关注的强影响点。
- B 在左上，残差越过了 $y=2.5$，但紧贴左侧、杠杆低，气泡不大，只是个离群点。
- A 在最右侧、贴着 $y=0$，杠杆最高但残差接近零，气泡小到几乎看不见，杠杆高却没有影响。

要盯的就是那些既靠右、又远离中线、气泡还大的点。

## 剔除强影响点，方程变了多少

既然 C 被诊断出来，最直接的检验就是把它删掉，看系数变了多少：

```{r coef-table}
comp <- tibble(
  term       = c("Intercept (b0)", "Slope (b1)"),
  full       = round(coef(fit), 2),
  without_C  = round(coef(fit_noC), 2)
) |>
  mutate(change_pct = round((without_C - full) / abs(full) * 100, 1))

knitr::kable(comp, col.names = c("Coefficient", "All data", "Without C", "Change (%)"),
             align = "lrrr")
```

斜率从 `r sprintf("%.2f", coef(fit)[2])` 抬到 `r sprintf("%.2f", coef(fit_noC)[2])`（约 +`r sprintf("%.0f", (coef(fit_noC)[2]-coef(fit)[2])/abs(coef(fit)[2])*100)`%），截距也明显回落。一个点就能改变"$x$ 每增加一个单位、$y$ 平均变化多少"这个结论。样本量小的时候，这种点必须查清楚：是录入错误，是量纲不一致，还是本就不该纳入的异常记录。

> 顺带一提：样本量很大时，单个点几乎不可能有这么大权重，专门找强影响点的意义有限；但在异常检测里，找出这种点恰恰是目的本身。

## 再细一点：三个删除诊断

除了上面三个，实务里还常用到三个更细的诊断量。它们的思路一致：删掉第 $i$ 个点重新拟合，看某个量变化了多少。用 `influence.measures(fit)` 一行就能全部算出来，这里为了对照各自的阈值分开算。

**DFFITS** 衡量删掉第 $i$ 点后，它自己的拟合值变化了多少个标准误：

$$
\text{DFFITS}_i = \frac{\hat{y}_i - \hat{y}_{(i)}}{\hat\sigma_{(i)} \sqrt{h_{ii}}}
$$

经验阈值 $2\sqrt{(p+1)/n}$。它和库克距离高度相关，相当于从单点自身拟合值的角度看同一件事。

**DFBETAS** 更细，衡量删掉第 $i$ 点后，每个回归系数各自变化了多少个标准误：

$$
\text{DFBETAS}_{j,i} = \frac{\hat\beta_j - \hat\beta_{j(i)}}{\hat\sigma_{(i)} \sqrt{(X^{\top} X)^{-1}_{jj}}}
$$

经验阈值 $2/\sqrt{n}$。它的好处是能定位到具体系数，告诉你这个点动的是哪一个 $\beta$：

```{r fig-dfbetas, fig.height=3.8}
dfb <- dfbetas(fit) |>
  as_tibble() |>
  rename(`Intercept (b0)` = `(Intercept)`, `Slope (b1)` = x) |>
  mutate(obs = dat$obs, label = dat$label) |>
  pivot_longer(c(`Intercept (b0)`, `Slope (b1)`),
               names_to = "coef", values_to = "dfbetas")

ggplot(dfb, aes(obs, dfbetas)) +
  geom_hline(yintercept = c(-thr$dfbetas, thr$dfbetas), linetype = 2,
             color = palette_blog["ink"], linewidth = 0.4) +
  geom_segment(aes(xend = obs, yend = 0, color = label), linewidth = 0.4) +
  geom_point(aes(color = label, size = label != "normal")) +
  geom_text(data = filter(dfb, label != "normal"),
            aes(label = label, color = label),
            vjust = -0.7, fontface = "bold", size = 3, show.legend = FALSE) +
  scale_color_manual(values = hl, guide = "none") +
  scale_size_manual(values = c(1.4, 2.6), guide = "none") +
  facet_wrap(~ coef) +
  labs(title = "DFBETAS pinpoints which coefficient a point distorts",
       x = "observation index", y = "DFBETAS") +
  theme_blog()
```

两个面板里都是 C 冲得最远，截距和斜率都被它拽出了阈值带，A、B 基本没动。这和前面"删掉 C 系数大变"是一致的。

**COVRATIO** 换了个角度，衡量删掉第 $i$ 点后，系数估计的精度（协方差矩阵的行列式）变化多少：

$$
\text{COVRATIO}_i = \frac{\det\big(\hat\sigma_{(i)}^{2} (X_{(i)}^{\top} X_{(i)})^{-1}\big)}{\det\big(\hat\sigma^{2} (X^{\top} X)^{-1}\big)}
$$

经验阈值是偏离 1 的幅度超过 $3(p+1)/n$，即 $\lvert \text{COVRATIO}_i - 1 \rvert \ge 3(p+1)/n$。读法和前面几个不太一样：

- $\text{COVRATIO}_i > 1$：删掉这个点，估计会变差，说明它在帮着提升精度；
- $\text{COVRATIO}_i < 1$：删掉它，估计反而变好，说明它在拖累精度。

```{r fig-covratio, fig.height=3.6}
band <- c(1 - thr$covr, 1 + thr$covr)
ggplot(d, aes(obs, covratio)) +
  annotate("rect", xmin = -Inf, xmax = Inf, ymin = band[1], ymax = band[2],
           fill = palette_blog["ash"], alpha = 0.15) +
  geom_hline(yintercept = 1, linetype = 2,
             color = palette_blog["ink"], linewidth = 0.4) +
  geom_point(aes(color = label, size = label != "normal")) +
  geom_text(data = filter(d, label != "normal"),
            aes(label = label, color = label),
            vjust = -0.9, fontface = "bold", size = 3.4, show.legend = FALSE) +
  scale_color_manual(values = hl, guide = "none") +
  scale_size_manual(values = c(1.6, 3), guide = "none") +
  labs(title = "COVRATIO separates good leverage from bad",
       subtitle = "Above the band = improves precision; below = hurts precision",
       x = "observation index", y = "COVRATIO") +
  theme_blog()
```

这张图有意思的地方在 A：前面几个指标都放过了它，只有 COVRATIO 把它标了出来。A 的 COVRATIO 有 `r sprintf("%.2f", d$covratio[d$label=="A"])`（在带子上方），说明这个高杠杆点其实在帮着稳住估计，删了反而更不准，是个"好"的杠杆点。C 则掉到 `r sprintf("%.2f", d$covratio[d$label=="C"])`（在带子下方很远），删掉它精度会明显变好，是要处理的坏点。

把三个点在所有指标上的表现汇成一张表，看哪种点被哪个指标抓到：

```{r summary-table}
mark <- function(cond) ifelse(cond, "✔", "–")

summ <- d |>
  filter(label != "normal") |>
  transmute(
    Point            = label,
    `Std. residual`  = mark(abs(std_resid) > 2.5),
    `Leverage`       = mark(hat > thr$hat),
    `Cook's D`       = mark(cooks > thr$cooks),
    DFFITS           = mark(abs(dffits) > thr$dffits),
    COVRATIO         = mark(abs(covratio - 1) > thr$covr)
  )

knitr::kable(summ, align = "lccccc")
```

- A（高杠杆点）：只有 COVRATIO 标记它，而且是"好"的那种，其余指标都判它无害。
- B（离群点）：残差类指标抓得到，杠杆类指标漏掉它。
- C（强影响点）：几乎所有指标同时点名，这才是真正要处理的点。

## 小结

回归诊断这套指标各有分工：

- 标准化残差看 $y$ 方向的偏离，也就是离群值；
- 杠杆值看 $x$ 方向的极端；
- 库克距离和 DFFITS 把两者合起来，衡量综合影响，用来锁定强影响点；
- DFBETAS 进一步定位到具体是哪个系数被动了；
- COVRATIO 从估计精度的角度看，还能分出好杠杆和坏杠杆。

实际用的时候顺序大致是这样：先看影响图，扫出可疑的大气泡；再用 DFBETAS 看它扰动了哪个系数、扰动多大；最后结合业务判断是修正、剔除还是保留。还有一点前面提过：样本量小的时候单个点足以改写结论，得查清楚；样本量很大时不必纠结单点，把找强影响点的力气留给真正需要它的异常检测更划算。
